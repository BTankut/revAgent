#!/usr/bin/env sh
set -eu

repo_root="$(git rev-parse --show-toplevel)"

if command -v pwsh >/dev/null 2>&1; then
  shell_bin="pwsh"
else
  shell_bin="powershell.exe"
fi

"$shell_bin" -NoProfile -ExecutionPolicy Bypass -File "$repo_root/scripts/test-pre-push.ps1" -RepoRoot "$repo_root"
