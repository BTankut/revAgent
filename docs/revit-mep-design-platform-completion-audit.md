# Revit MCP MEP Design Platform Completion Audit

## Objective Restatement

Apply `docs/revit-mep-design-platform-full-goal.md` on branch `feature/full-mep-design-platform-goal` without harming `main`, preserve existing dirty work, use plugin branch `feature/native-write-plan-executor`, and live-test through open Revit/MCP.

Concrete deliverables:

- Runtime MCP write-plan platform tools.
- Native plugin `execute_write_plan` executor.
- Workflow identity/eId state.
- Safety model and office standards config.
- Initial MEP domain engines.
- Skill/README/docs updates.
- Static tests, plugin build, MCP handshake, and live Revit validation.
- Branch push where possible, with `main` untouched.

## Prompt-to-Artifact Checklist

| Requirement | Evidence | Status |
|---|---|---|
| Read and apply `docs/revit-mep-design-platform-full-goal.md` | Source goal file remains on branch; architecture/validation/PR docs derive from its required flow and deliverables. | Done |
| Do not harm `main` | Work occurred on `feature/full-mep-design-platform-goal` and `feature/native-write-plan-executor`; `git status --short --branch` clean on both feature branches. | Done |
| Preserve dirty changes | Plugin repo's existing `send_code_to_revit` dirty files were kept and included with the native executor commit instead of reverted. | Done |
| Runtime existing six tools regress not intentionally changed | Existing tool files unchanged except registry imports; safe guard test still passes. | Done |
| New runtime write-plan tools list | Fresh MCP handshake against `C:\Users\BT\Projects\revit-mcp-runtime\build\index.js` listed 13 tools, including all seven new tools. `tool-registration.test.js` also asserts the existing six tools plus all seven write-plan/platform tools are registered. | Done |
| Typed write-plan schema/protocol | `kurulum/mcp-server/build/write-plan/schemas.js`, `validators.js`, `risk.js`, `previewFormatter.js`. | Done |
| `prepare_write_plan` invalid plan rejection | Tool behavior test returned `invalidSuccess: false`; unit test covers empty step rejection. | Done |
| `preview_write_plan` must not mutate model | Tool behavior test returned `previewMutates: false`; live preview re-read confirmed `Comments` unchanged. | Done |
| `commit_write_plan` rejects without approval/token | Tool behavior test returned `commitRejected: true`; commit tool checks token/explicit approval. | Done |
| Workflow eId mapping | `workflowStore.js`; native executor returns mappings; runtime hydrates `eId` targets from workflow state before preview/commit/verify; live preview resolved `duct-preview-001` to duct `1749785`. | Done |
| Office standards config | `office-standards/defaults.js`; HVAC live analysis returned missing standard blocker. | Done |
| Safety model | `risk.js`, commit-token gate, direct commit fallback disabled by default, skill checklist updated. | Done |
| Native plugin executor | Plugin repo `SampleCommandSet/Commands/WritePlan/*`; build passed for `Debug 2022|x64`. | Done |
| Native executor exposed by normal Revit command registry | Reflection showed the open registry initially lacked `execute_write_plan`; plugin command SDK mismatch was fixed; compat assembly was hot-registered in the active session; normal socket preview succeeded without direct fallback. After a clean Revit restart/reload, the `Revit MCP Switch` add-in command was clicked and the installed registry loaded `execute_write_plan` from `SampleCommandsetCompat\2022\SampleCommandSetCompat.dll` without the temporary hot-register path. | Done |
| Native executor live preview | Direct assembly fallback preview succeeded first; after SDK compatibility fix and hot-register, normal socket `execute_write_plan` preview also succeeded and did not mutate model. | Done |
| Native executor verification coverage | Expanded verifier build passed; verifier reads back set/clear/copy parameter values, type change, pin/unpin, delete, tag target/head position, copy translation, move expected LocationCurve/LocationPoint, rotate expected LocationCurve, align expected LocationCurve/LocationPoint, placed family symbol/level/location, view override projection color/line weight, resize, view hide/unhide, and target existence. Live verification failure behavior is proven. Normal socket preview is live-proven both before and after clean Revit restart/reload. | Done |
| Native executor live commit/verify on test model | User opened disposable model `rme_advanced_sample_project - Kopya` and explicitly approved commit testing. Plan `test-model-commit-1777956608565` set duct `392168` `Comments`; preview did not mutate, commit succeeded, verify succeeded, final readback matched. | Done |
| Native parameter operations | Runtime validation tests now explicitly cover valid/invalid `clear_parameter` and `copy_parameter_value`; plugin validator parity now checks parameter/type/view/move/place/resize payload requirements. Plugin unique build `SampleCommandSetParamVerifyTest` passed. Approved live plan `clear-parameter-live-1777980200006` created disposable duct `1021017`, preview left `Comments` unchanged, commit cleared it, native verify succeeded, cleanup deleted it, and duct count returned to `744`. Approved live plan `copy-parameter-live-1777980200007` created disposable source `1021019` and target `1021021`, preview left target `Comments` unchanged, commit copied the source value, the new operation-specific verifier matched expected/actual values, cleanup deleted both ducts, and duct count returned to `744`. A larger combined parameter scenario timed out through the MCP call but final read-only checks showed no `1021000+` duct ids and duct count remained `744`; the passing isolated tests are the acceptance evidence. | Done |
| Native type change operation | Approved live test plan `change-type-live-1777975200001` copied source duct `392168` to disposable element `1020985`, confirmed the copy started at type `142427`, preview left it unchanged, commit changed it to valid alternate type `142426`, native verify matched `expectedTypeId` and `actualTypeId` `142426`, cleanup deleted the copy, and final read-only check confirmed source duct `392168` still exists at original type `142427` while copy `1020985` no longer exists. Revit 2022 docs resolved `Element.ChangeTypeId`, `Element.GetTypeId`, `Element.GetValidTypes`, `DuctType`, and `ElementType.Name`. | Done |
| Native pin/unpin element operations | `pin_elements` / `unpin_elements` moved from future operations into the initial write-plan catalog with low risk classification, target validation, native executor commit support, and native verifier readback. Runtime write-plan tests passed in both the skill build and synced runtime build; plugin `Debug 2022|x64` build passed. Approved live reversible test on duct `392168` confirmed preview did not mutate, commit pinned, verify reported `actualPinned: true`, restore unpinned, restore verify succeeded, and final pinned state returned to original `false`. | Done |
| Native delete element operation | `delete_elements` moved from future operations into the initial write-plan catalog with `critical` risk classification, target validation, native executor commit support, and native verifier readback that confirms targets no longer exist. Runtime write-plan tests passed in both the skill build and synced runtime build; plugin `Debug 2022|x64` and unique test builds passed. Approved live test plan `delete-elements-live-1777969000001` created disposable duct `1020971`, preview left it in place, commit returned `deletedElementCount: 1`, verify succeeded, and final `inspect_elements` readback returned `Element not found: 1020971`. | Done |
| Native tag element operation | `tag_elements` moved from future operations into the initial write-plan catalog with `medium` risk classification, target/point validation, native `IndependentTag.Create` support, created tag mappings, and native verifier readback that checks tagged local target ids and tag head point. Runtime write-plan tests passed in both the skill build and synced runtime build; plugin `Debug 2022|x64` unique output and unique assembly builds passed. Revit 2022 docs resolved `IndependentTag.Create`, `Reference(Element)`, `TagMode`, `TagOrientation`, `GetTaggedLocalElementIds`, `TagHeadPosition`, and `OST_DuctTags`. Direct API probe created/deleted tag `1020983`. Approved live native plan `tag-elements-live-1777973200002` preview did not mutate tag count (`1409` before/after), commit created tag `1020984` on duct `392168` with duct tag type `102763`, verify succeeded with target match and tag head point error `2.31e-13`, cleanup deleted it, and final read-only check returned `tag1020984Exists: false` plus no recent `1020980+` tags. | Done |
| Native copy element operation | `copy_elements` moved from future operations into the initial write-plan catalog with `medium` risk classification, target/vector validation, native executor commit support, created-element mappings, and native verifier readback that checks copied element existence and requested LocationCurve/LocationPoint translation when geometry is available. Runtime write-plan tests passed in both the skill build and synced runtime build; plugin `Debug 2022|x64` and unique test builds passed. Approved live test plan `copy-elements-live-1777970200001` copied source duct `392168` to `1020974`, verify succeeded with requested `2 m` translation, source remained, cleanup deleted the copy, and final `inspect_elements` found source `392168` while reporting `Element not found: 1020974`. | Done |
| Native move element operation | `move_elements` now has operation-specific native verifier readback for expected `LocationCurve` / `LocationPoint` geometry, and runtime validation tests explicitly cover valid/invalid move payloads. Plugin unique build `SampleCommandSetMovePlaceTestV4` passed. Revit 2022 docs resolved `ElementTransformUtils.MoveElements`. Approved live plan `move-elements-live-1777981200001` created disposable duct `1021027`, preview left its endpoints unchanged, commit moved it by `2 ft`, direct readback and native verify matched the expected moved curve, cleanup deleted it, and final duct count returned to `744`. | Done |
| Native view hide/unhide operations | `view_hide_elements` and `view_unhide_elements` have native commit support and verifier readback through `Element.IsHidden(view)`. Revit 2022 docs resolved `Element.IsHidden`, `View.HideElements`, and `View.UnhideElements`. Approved live plan `view-hide-unhide-live-1777981200002` created disposable duct `1021029`, hide preview left it visible, hide commit set hidden state true in view `378466`, hide verify succeeded, unhide preview left hidden state true, unhide commit restored hidden state false, unhide verify succeeded, cleanup deleted it, and final duct count returned to `744`. | Done |
| Native place family instance operation | `place_family_instance` now requires a point plus family symbol identity in runtime validation, uses level-aware placement with runtime reflection fallback for Revit 2022 method compatibility, corrects the placed `LocationPoint` after creation, and verifies placed family instance symbol/level/location. Plugin unique build `SampleCommandSetMovePlaceTestV4` passed. Revit 2022 docs resolved `NewFamilyInstance`, `FamilySymbol`, `FamilyInstance`, `FamilyPlacementType`, and `LocationPoint`. Direct probe identified mechanical equipment symbol `386027` and confirmed level-based placement behavior. Approved live plan `place-family-instance-live-1777981200005` preview left mechanical equipment count at `47`, commit placed instance `1021031` with symbol `386027`, level `378117`, and expected location `82,168,10.668449717049469 ft`, native verify matched symbol/level/location, cleanup deleted it, and final mechanical equipment count returned to `47`. | Done |
| Native create duct run operation | Direct `create_duct_run` and `create_pipe_run` validation now requires `points`, `systemTypeId`, curve type id or `typeId`, and `levelId`; native direct run creation also accepts `typeId` alias. Runtime write-plan tests passed in both the skill build and synced runtime build; plugin `Debug 2022|x64` output and unique assembly builds passed. Revit 2022 docs resolved `Duct.Create`, `MEPCurve.MEPSystem`, `Element.LevelId`, `LocationCurve.Curve`, `Level.GetNearestLevelId`, and `RBS_START_LEVEL_PARAM`. Direct API probe created/deleted duct `1020987`. Approved live native plan `create-duct-run-live-1777976200002` preview did not mutate duct count (`744` before/after), commit created duct `1020989`, readback matched type `142427`, length `5 ft`, width/height `0.984251968503937 ft`, native verify succeeded, cleanup deleted it, and final read-only check returned `createdExists: false`, `ductCount: 744`, `restored: true`. | Done |
| Native resize duct operation | Approved live test plan `resize-duct-live-1777977200001` created disposable duct `1020991`, set initial size to `0.984251968503937 x 0.984251968503937 ft`, preview left size unchanged, commit resized it to `1.31233595800525 x 0.820209973753281 ft`, native verify matched Width and Height internal values, cleanup deleted it, and final read-only check returned `createdExists: false`, `ductCount: 744`, `restored: true`. | Done |
| Native create pipe run operation | Direct `create_pipe_run` validation requires `points`, `systemTypeId`, `pipeTypeId` or `typeId`, and `levelId`; native direct pipe creation accepts the shared `typeId` alias. Revit 2022 docs resolved `Pipe.Create`, `PipeType`, `MEPCurve.MEPSystem`, `Element.LevelId`, `LocationCurve.Curve`, and `Level.GetNearestLevelId`. Direct API probe created/deleted pipe `1020995`. Approved live native plan `create-pipe-run-live-1777979200001` preview did not mutate pipe count (`488` before/after), commit created pipe `1021001` with type `142438`, length `5 ft`, diameter `0.492125984251969 ft`, native verify succeeded, cleanup deleted it, and final read-only check returned `createPipeExists: false`, `pipeCount: 488`, `restored: true`. | Done |
| Native resize pipe operation | Approved live test plan `resize-pipe-live-1777979200002` created disposable pipe `1021004`, set initial diameter to `0.492125984251969 ft`, preview left diameter unchanged, commit resized it to `0.328083989501312 ft`, native verify matched the Diameter internal value, cleanup deleted it, and final read-only check returned `resizePipeExists: false`, `pipeCount: 488`, `restored: true`. | Done |
| Native verification failure case | Approved live read-only verify plan `verification-failure-live-1777978200001` intentionally expected wrong Width/Height on source duct `392168`; native verify returned `success: false`, emitted mismatch errors including `one or more resize parameters do not match expected values`, and source duct size stayed `0.984251968503937 x 0.984251968503937 ft`. | Done |
| Native transaction rollback behavior | Approved live rollback plan `rollback-live-1777978200002` used a two-step commit: valid `create_duct_run` followed by `set_parameter` on missing element `999999999`. Native commit returned `success: false` with `Target element not found.`, rollback removed the first-step duct creation, duct count stayed `744`, and no `1020990+` duct ids remained. | Done |
| Native rotate element operation | `rotate_elements` moved from future operations into the initial write-plan catalog with `medium` risk classification, target/axis/angle validation, native executor commit support, and native verifier readback that checks expected LocationCurve endpoints when supplied. Runtime write-plan tests passed in both the skill build and synced runtime build; plugin `Debug 2022|x64` and unique test builds passed. Approved live test plan `rotate-elements-live-1777971200002` created disposable duct `1020976`, preview left it in place, commit rotated it `90` degrees around the requested vertical axis, verify matched expected endpoints, cleanup deleted the duct, and final `inspect_elements` readback returned `Element not found: 1020976`. | Done |
| Native align element operation | `align_elements` moved from future operations into the initial write-plan catalog with `medium` risk classification, target/sourcePoint/targetPoint validation, constrained-axis translation through native `MoveElements`, and native verifier readback that checks expected LocationCurve or LocationPoint geometry when supplied. Runtime write-plan tests passed in both the skill build and synced runtime build; plugin `Debug 2022|x64` unique output and unique assembly builds passed. Normal plugin output build was blocked only by the open Revit process locking the already-loaded DLL. Approved live test plan `align-elements-live-1777972200003` copied source duct `392168` to disposable duct `1020981`, preview did not move it, commit aligned it by the requested x-axis translation, verify succeeded with expected `LocationCurve` endpoints and `0` internal start-point error, cleanup deleted it, and final read-only check returned `exists: false`. | Done |
| Native view override operation | `view_apply_overrides` now requires at least `projectionLineColor` or `projectionLineWeight`, has runtime validation coverage in both the skill build and synced runtime build, and has native verifier readback for projection line color/weight with invalid-color-safe failure reporting. Plugin `Debug 2022|x64` output and unique assembly builds passed. Revit 2022 docs resolved `View.GetElementOverrides`, `View.SetElementOverrides`, `OverrideGraphicSettings`, `ProjectionLineColor`, `ProjectionLineWeight`, `SetProjectionLineColor`, `SetProjectionLineWeight`, and `Color.IsValid`. Approved live test plan `view-override-live-1777974200002` on duct `392168` in view `378466` confirmed preview did not mutate the original `invalid;w=-1` override state, commit applied `255,0,0;w=5`, native verify succeeded, restore returned `invalid;w=-1`, and final read-only check reported `restored: true`. | Done |
| HVAC duct analysis real model read-only | Live `analyze_mep_system` read `8840` ducts, `7996.625 m` duct length, `41735` connectors, `708` open connectors. | Done |
| Hydronic/domestic/sanitary/fire/clash/equipment foundations | Foundation modules exist and return assumptions/missing standards; HVAC/fire/hydronic have live/read collectors; domestic water now covers fixture-unit summation, demand interpolation, pressure-loss basis, pipe sizing proposal, and recirculation continuity; sanitary/storm now covers slope/reverse-slope validation, gravity pipe sizing proposal, rational-method storm runoff and pipe sizing proposal, branch-to-stack reachability, and vent continuity; fire/sprinkler now covers sprinkler coverage, fire cabinet hose-reach screening, fire cabinet demand basis, and fire pump flow/pressure basis; all domain foundations expose deterministic calculation/issue examples with `canCommit: false`. | Done |
| MEP graph foundation | HVAC and hydronic live connector graph summaries read `Connector.AllRefs`, node counts, unique element edge counts, and open connector samples with `0` AllRefs errors. Targeted read-only live connector pathfinding was added through `networkRootElementId` / `networkTerminalElementIds` and validated in the disposable model on duct `392168`. | Done |
| Engineering validation calculations | `engineering-calculations.test.js` checks duct/pipe calculations, pipe resistance coefficient calibration, hydronic pipe resize proposal rows, validator-approved `resize_pipe` step generation, rooted tree branch flow aggregation, weighted shortest-path traversal, least-loss flow direction inference, fan pressure basis, pump head basis, hydronic balancing loss, single-loop and coupled two-loop Hardy-Cross balancing, critical path/critical circuit selection, and cycle/disconnected warnings; `domain-foundation-calculations.test.js` checks domestic water, sanitary/storm, fire/sprinkler, clash, equipment, pipe-sizing report rows, reporting foundations, and top-level `writePlanProposal` aggregation/validation for proposal-only `resize_duct` and `resize_pipe` steps. | Done |
| Runtime report export workflow | `export_boq_report` and `export_clash_report` write-plans are now initial non-model-mutating operations handled by `runtimeReportExecutor.js`; approved commit writes CSV/JSON files, returns `writesFiles: true`, and keeps `mutateModel: false`. Runtime verify re-checks file existence/content without native Revit execution. Unit test writes and verifies a CSV report file in both the skill build and synced runtime build. | Done |
| Live-model BOQ/report population | `boqOnly` collectors populate report rows from live Revit count/length summaries without connector traversal. Live hydronic probe returned `488` pipes, `489` pipe fittings, `47` mechanical equipment, `930.684 m` pipe length, and `12` BOQ rows. | Done |
| Real-model hydraulic resistance extraction/calibration | `hydraulicResistanceOnly` reads live Revit pipe length/diameter samples and `calibratePipeResistanceSamples` converts them to resistance coefficients and report rows. Live probe returned `5` pipe samples; first sample was element `513756`, `Hydronic Supply`, `4.795 m`, `150 mm`, coefficient `1.683 Pa/(L/s)^2` at `1 L/s`; report builder returned `5` hydraulic resistance rows. | Done |
| Real-model fitting/accessory/equipment local-loss extraction | `localLossOnly` reads live HVAC/hydronic fitting, accessory, terminal, and equipment loss-like parameters and `summarizeLocalLossSamples` converts them into `local_loss` and `local_loss_pressure` report rows. Live probe returned HVAC `5` samples / `15` rows / first numeric `0.903 Pa` on duct fitting `392203`; hydronic `5` samples / `30` rows / first numeric `193.936 Pa` on pipe fitting `513769`. | Done |
| Local-loss fan/pump pressure basis contribution | Numeric pressure-drop parameters are aggregated by system/category and passed into basis calculations as `localLossPressurePa`. Live probe carried HVAC `0.903 Pa` into `liveLocalLossFanPressureBasis` and hydronic `8925.566 Pa` (`8.926 kPa`) into `liveLocalLossPumpHeadBasis`; runtime synced build also returned HVAC `0.903 Pa` and hydronic `4.560 kPa` for a 3-sample probe. | Done |
| HVAC duct resize proposal foundation | `buildHvacDuctResizeProposal` now combines live duct length/size samples, supplied or model-read design airflows, HVAC office equal-friction/velocity limits, and complete critical-path local-loss pressure context into `hvac_duct_sizing_proposal` rows plus proposal-only `resize_duct` write-plan steps with `canCommit: false`. Runtime report builder emits `duct_sizing` rows/CSV. Tests validate the generated `resize_duct` step through `validateStep`, analyzer fake-executor coverage verifies read-only duct sample collection, and live read-only micro-probe on path `[392199, 392203, 392200]` read fitting `392203` pressure drop `0.903 Pa`, proposed duct `392199` from `450 x 200 mm` to `200 x 300 mm` for test design flow `900 m3/h`, emitted `resize-duct-392199`, and final counts stayed `744` ducts / `488` pipes. Live model-flow probe confirmed `Flow` conversion from internal ft3/s to m3/h; `ductSizingOnly` with no supplied design-flow map read duct `392168` display `350.0 L/s` / `1260 m3/h` and produced proposal-only rows with `canCommit: false`. | Done |
| Hydronic pipe resize proposal foundation | `buildHydronicPipeResizeProposal` now combines live pipe length/diameter samples, supplied or model-read design flows, hydronic office velocity/friction limits, and complete critical-circuit local-loss pressure context into `hydronic_pipe_sizing_proposal` rows plus proposal-only `resize_pipe` write-plan steps with `canCommit: false`. Runtime report builder emits `pipe_sizing` rows/CSV. Tests validate complete and incomplete local-loss gates and run the generated step through `validateStep`. The `localLossFromNetworkPath` hydronic branch now optionally performs a pipe resistance read and returns `resistanceCalibration` plus `pipeSizingProposal`; fake-executor coverage verifies the read sequence and selected-path target filtering. Live read-only probes on the disposable model sampled pipes `513756`, `513770`, and `513840`, and a smaller explicit critical-circuit micro-probe confirmed pipe `513756` -> fitting `513769` -> pipe `513637`, read `513769` pressure drop `193.936 Pa`, proposed `513756` from `150 mm` to `80 mm`, emitted `resize-pipe-513756`, and final counts stayed `488` pipes / `744` ducts. Live model-flow probe found nonzero pipe `Flow`, including `513840` display `30.6 L/s`; `hydraulicResistanceOnly` with no supplied design-flow map produced 10 proposal-only pipe sizing rows with `canCommit: false`. | Done |
| Analysis write-plan proposal handoff | `analyze_mep_system` now returns top-level `writePlanProposal` by aggregating nested domain proposal `writePlanSteps` from HVAC duct sizing, hydronic pipe sizing, and equipment schedule proposals into a normalized plan. The helper validates each step, keeps `canCommit: false`, and states that generated plans must still pass `prepare_write_plan`, `preview_write_plan`, explicit approval, `commit_write_plan`, and `verify_write_plan`. | Done |
| Targeted critical-path local-loss extraction input | `analyze_mep_system` now accepts `localLossElementIds` so a prior connector/pathfinding or critical-path step can provide exact fitting/accessory/equipment ids instead of first-sample category collection. Unit test covers generated targeted collector code. Direct live targeted probe confirmed HVAC fitting `392203` pressure drop `0.903 Pa` and hydronic fitting `513769` pressure drop `193.936 Pa`. | Done |
| Connector-path-derived local-loss targeting | `analyze_mep_system` now accepts `localLossFromNetworkPath`; HVAC/hydronic analysis uses a shared two-stage `readPathTargetedLocalLosses` helper that first runs live connector pathfinding, reads local-loss samples for all reachable candidate path ids, selects by explicit total local-loss pressure drop when numeric samples are available, falls back to maximum hop count otherwise, and then re-runs local-loss extraction against that selected path's element ids. Unit tests cover `selectCriticalConnectorPath`, pressure-based ranking, path id de-duplication, `targetedByCriticalPath`, the helper's three read calls with fake executor assertions, long explicit target sets that raise the sample limit beyond the general 200-sample cap, analysis-level warnings for `uninspectedTargetCount` / `truncatedBySampleLimit`, selected-path pressure consistency checks between ranking pressure and final extraction pressure, and `local_loss_selected_path_pressure_check` report/CSV rows; live Revit evidence selected terminal `392200` on path `392199 -> 392203 -> 392200`, sampled duct fitting `392203`, skipped the two duct targets, and returned `Pressure Drop` `0.903187266396887 Pa`. | Done |
| Clash/reroute solving foundation | `solveOrthogonalReroute` generates y/z bypass candidates for x-directed route segments, validates expanded obstacle clearance, and selects the shortest valid candidate. Runtime example produced `4` candidates, selected a valid candidate with `0` violations and `1.025 m` added length; `canCommit: false`. | Done |
| Native reroute commit verification foundation | `commit_reroute` is now an initial native operation with runtime validation/risk coverage. Live disposable-model test plan `reroute-commit-1777960000001` created duct segments `1020923`, `1020925`, and `1020927`; native verify re-read all targets and matched `3` actual segments to `3` expected segments with expected/actual total length `15.292563747898 ft`. Clearance test plan `reroute-clearance-1777962600000` created ducts `1020932` and `1020934`, verified length/count, and returned `clearanceViolationCount: 0` against supplied expanded obstacle boxes. | Done |
| Native reroute source replacement foundation | `commit_reroute` now supports `deleteSource` / `replaceSource`, validates that a `sourceElementId` is supplied, deletes the source after replacement route creation, and verifies source deletion during readback. Approved live test plan `reroute-source-replace-1777962600000` created disposable source duct `1020938`, committed replacement duct segments `1020941` and `1020943`, deleted source `1020938`, verified expected/actual segment count `2`, expected/actual total length `6.5616797899999995 ft`, `clearanceViolationCount: 0`, and `sourceReplacementCheck.exists: false`; final readback confirmed both new ducts at `300 x 300 mm` and source `1020938` not found. | Done |
| Native reroute reconnection foundation | `commit_reroute` now supports `reconnect`, `reconnectSource`, `connectSegments`, `requireReconnect`, and `expectedSourceConnectionCount`; it captures source connector references before deletion, reconnects replacement route segment endpoints, reconnects source neighbors, and verifies physical external duct/pipe-style references while filtering system proxy references. Approved live test plan `reroute-reconnect-1777965200000` used connected chain left `1020947` -> source `1020949` -> right `1020951`, deleted source `1020949`, created ducts `1020954` and `1020956`, returned commit `segmentConnectionCount: 1`, `sourceConnectionCount: 2`, verify `externalConnectionCount: 2` to physical ducts `1020947` and `1020951`, and final readback showed both replacement ducts at `300 x 300 mm` with `openConnectorCount: 0`. Additional approved L-shaped route test `reroute-l-reconnect-1777967000000` used chain left `1020958` -> source `1020960` -> right `1020962`, created replacement ducts `1020965`, `1020967`, and `1020969`, verified `segmentConnectionCount: 2`, `externalConnectionCount: 2`, source deleted, clearance `0`, and final readback showed all three replacement ducts at `300 x 300 mm` with `openConnectorCount: 0`. Plugin patch `09/10` adds verify readback for route-connected duct/pipe fitting owners via `routeFittingRefCount` and `routeFittingRefs`; plugin `Debug 2022|x64` build passed, and live read-only probe on route ducts `1020965`, `1020967`, `1020969` returned `routeFittingRefCount: 0`, explicitly documenting that Revit did not create separate fitting owners for that L-route test. | Done |
| Looped hydraulic solving foundation | `solveHardyCrossLoop` performs single-loop Hardy-Cross balancing from supplied resistance coefficients and signed initial flows; runtime example converged in `3` iterations with final residual `0.000004 Pa`. `solveHardyCrossNetwork` performs sequential coupled-loop balancing; runtime example converged across `2` loops / `5` edges in `5` iterations with max residual `0.000465 Pa`. `canCommit: false`. | Done |
| Equipment schedule/report proposal and approved execution | `buildEquipmentScheduleProposal` emits equipment selection report rows and low-risk `set_parameter` write-plan proposal steps without replacing equipment. Runtime example selected `fan-b` and targeted `eId: supply-fan-001`. Live disposable-model test plan `equipment-schedule-update-1777960000000` updated Mechanical Equipment `386031` `Comments`; native preview, commit, verify, and final readback all succeeded. | Done |
| Full engineering engines | Deterministic foundations now cover branch flow aggregation, weighted graph traversal, least-loss flow direction inference, fan/pump basis, live hydronic resistance extraction/calibration, HVAC duct resize and hydronic pipe resize proposal rows plus proposal-only `resize_duct` / `resize_pipe` steps from explicit or path-targeted local-loss context, top-level `writePlanProposal` handoff aggregation for generated proposal steps, live fitting/accessory/equipment local-loss extraction/reporting, targeted local-loss extraction input for known critical-path/circuit ids, connector-path-derived local-loss targeting, local-loss pressure contribution into fan/pump basis, hydronic critical-circuit balancing loss, single/coupled-loop Hardy-Cross balancing, multi-candidate clash reroute solving, native reroute geometry/count/length/clearance/source-replacement/reconnection/fitting-reference verify, native parameter set/clear/copy verification, native type change verification, native pin/unpin protection operations, native critical delete verification, native tag annotation verification, native copy/mapping/translation verification, native move geometry verification, native place family instance symbol/level/location verification, native view hide/unhide verification, native direct duct-run creation verification, native duct resize verification, native direct pipe-run creation verification, native pipe resize verification, native verification-failure handling, native rollback behavior, native rotate geometry verification, native align geometry verification, native view override verification, equipment schedule/report proposals with approved native note update execution, native model schedule population, live BOQ/report rows, and approved report file export. Production-calibrated final sizing from complete project-critical path datasets and broader production reroute fitting behavior are not complete. | Partially Done |
| Native model schedule population | `create_schedule_or_update_schedule` is now an initial native operation with runtime validation/risk coverage. Live disposable-model test plan `schedule-population-1777960000000` created schedule `Codex MEP Equipment Schedule 2026-05-05` as element `1020916`, added `Family and Type`, `Mark`, and `Level`, verified through native readback, and final readback matched. | Done |
| Skill update | `SKILL.md` version `0.5.0`; write-plan workflow documented. | Done |
| README/docs update | README updated; architecture, validation, PR summary, audit docs added. | Done |
| Static tests | JS syntax, safe guard test, tool registration test, write-plan schema/state/risk test, engineering calculation test, and domain foundation calculation test passed in both the skill build and synced runtime build. | Done |
| Plugin build test | `dotnet msbuild SampleCommandSet\SampleCommandSet.csproj /p:Configuration="Debug 2022" /p:Platform=x64 /m:1` passed. | Done |
| Docs MCP live validation | Revit 2022 API docs resolved `Duct.Create`, `Pipe.Create`, `PipeType`, `MoveElements`, `CopyElements`, `RotateElements`, `NewFamilyInstance`, `FamilySymbol`, `FamilyInstance`, `FamilyPlacementType`, `Element.IsHidden`, `View.HideElements`, `View.UnhideElements`, `Element.ChangeTypeId`, `Element.GetTypeId`, `Element.GetValidTypes`, `DuctType`, `ElementType.Name`, `MEPCurve.MEPSystem`, `Element.LevelId`, `LocationCurve.Curve`, `Level.GetNearestLevelId`, `RBS_START_LEVEL_PARAM`, `IndependentTag.Create`, `Reference`, `TagMode`, `TagOrientation`, `OST_DuctTags`, `LocationCurve`, `LocationPoint`, `View.GetElementOverrides`, `View.SetElementOverrides`, `OverrideGraphicSettings`, `ProjectionLineColor`, `ProjectionLineWeight`, `SetProjectionLineColor`, `SetProjectionLineWeight`, `Color.IsValid`, `UnitUtils`. | Done |
| Runtime MCP initialize | Fresh registered runtime handshake succeeded and listed 13 tools. | Done |
| Revit live connection | `get_revit_session_context`, `get_active_view_context`, `inspect_parameter_schema`, `analyze_mep_system`, direct native preview/verify fallback, and normal socket native preview tested. | Done |
| Current runtime normal socket native preview | Fresh stdio handshake against `C:\Users\BT\Projects\revit-mcp-runtime\build\index.js` listed all 13 tools. A `preview_write_plan` call with `useNativeExecutor: true` on duct `392168` used normal `execute_write_plan` (`directAssemblyFallback: false`), returned one preview row, reported `mutateModel: false`, and post-preview `inspect_elements` confirmed the source `Comments` value was unchanged. | Done |
| Clean Revit restart/reload native registry proof | Revit was restarted on temporary model copy `rme_advanced_sample_project_codex_restart_test.rvt`; the `Add-Ins` tab was opened and `Revit MCP Switch` was clicked through UI Automation. `get_revit_session_context` reconnected to the new process. A fresh stdio runtime handshake listed all 13 tools, and plan `restart-native-preview-1777980684540` called normal native `preview_write_plan` on duct `392168` with `warnings: []`, one preview row, `mutateModel: false`, and no direct-fallback marker. Post-preview `Comments` readback stayed empty. | Done |
| Disposable live commit sanity after proposal aggregation | After the `writePlanProposal` aggregation change, the user re-approved commit testing on `rme_advanced_sample_project - Kopya`. A read-only preview confirmed duct count `744`; commit copied source duct `392168` to disposable duct `1021032` and set `Comments = Codex disposable live commit 2026-05-05T00:00:00Z`; inspection verified the copied value and source duct `392168` stayed unchanged; cleanup deleted `1021032`; final counts returned to `744` ducts and `488` pipes. | Done |
| Branch pushed | Skill branch pushed through `origin/feature/full-mep-design-platform-goal`. | Done |
| Plugin branch pushed | Push failed because upstream plugin repo is archived/read-only and returns HTTP 403; plugin changes are exported as `docs/revit-mcp-plugin-native-write-plan-executor.patch`. The patch artifact now contains `19/19` patches and was verified with `git am --3way` on a temporary plugin `main` worktree. | Blocked |
| Clear PR/handoff summary | `docs/revit-mep-design-platform-pr-summary.md`. | Done |

## Current Blocking Items

- Plugin repo needs a writable fork/remote before branch push can succeed.
- Full production engineering engines remain beyond the current implemented foundation: local-loss parameters are now extracted, targeted by explicit ids or selected connector paths, reported, carried into fan/pump basis, and can feed proposal-only hydronic `resize_pipe` steps from live pipe samples, but production-calibrated final sizing from complete project-critical datasets plus broader production reroute fitting behavior are still outstanding.

## Completion Decision

Do not mark the goal complete yet. The platform foundation is implemented, normal native socket preview/verify works after clean Revit restart/reload, the user-approved disposable-model write-plan commit/verify acceptance test passed, and targeted live connector pathfinding plus flow-direction, fan/pump, hydronic resistance calibration/balancing, hydronic resize proposal rows, explicit-id and connector-path-targeted local-loss extraction/reporting/pressure-basis contribution, Hardy-Cross hydraulic solving, clash reroute solving, native reroute geometry/count/length/clearance/source-replacement/reconnection verify, native parameter set/clear/copy verification, native type change verification, native pin/unpin protection operations, native critical delete verification, native tag annotation verification, native copy/mapping/translation verification, native move geometry verification, native place family instance symbol/level/location verification, native view hide/unhide verification, native direct duct-run creation verification, native duct resize verification, native direct pipe-run creation verification, native pipe resize verification, native verification-failure handling, native rollback behavior, native rotate geometry verification, native align geometry verification, native view override verification, equipment schedule proposal/execution, native model schedule population, live BOQ, and report export/verify foundations are now in place. Remaining blockers are outside the current committed foundation: the plugin upstream is archived/read-only so its branch cannot be pushed, and full production engineering engines still need production-calibrated final sizing from complete project-critical local-loss datasets plus broader production reroute fitting behavior.
