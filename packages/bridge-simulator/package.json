{
  "name": "@revagent/bridge-simulator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "revagent-bridge-simulator": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "npm run build --workspace @revagent/protocol && npm run build --workspace @revagent/addin-loopback-fixture && npm run build:self",
    "build:self": "node ../../node_modules/typescript/lib/tsc.js -p tsconfig.json",
    "demo:crash": "node dist/cli.js crash-recovery",
    "lint": "eslint src tests",
    "typecheck": "npm run build --workspace @revagent/protocol && npm run build --workspace @revagent/addin-loopback-fixture && tsc -p tsconfig.json --noEmit && tsc -p tsconfig.tests.json",
    "test": "npm run build && vitest run",
    "test:determinism": "npm run build && vitest run --testTimeout=15000 --hookTimeout=15000 --reporter=json --outputFile=.bridge-simulator-run-1.json && vitest run --testTimeout=15000 --hookTimeout=15000 --reporter=json --outputFile=.bridge-simulator-run-2.json && vitest run --testTimeout=15000 --hookTimeout=15000 --reporter=json --outputFile=.bridge-simulator-run-3.json && node scripts/compare-runs.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@revagent/addin-loopback-fixture": "0.0.0",
    "@revagent/protocol": "0.0.0",
    "better-sqlite3": "13.0.3",
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/ws": "^8.18.1"
  }
}
