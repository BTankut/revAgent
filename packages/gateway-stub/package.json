{
  "name": "@revagent/gateway-stub",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "revagent-rbp-gateway-stub": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "npm run build --workspace @revagent/protocol && tsc -p tsconfig.json",
    "lint": "eslint src tests",
    "prepack": "npm run build",
    "start": "node dist/cli.js",
    "typecheck": "npm run build --workspace @revagent/protocol && tsc -p tsconfig.json --noEmit",
    "test": "npm run build && vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@revagent/protocol": "0.0.0",
    "ws": "^8.20.1"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1"
  }
}
